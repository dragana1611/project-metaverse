"use client";

import { motion } from "framer-motion";

import styles from "../styles";

const Navbar = () => <nav>navbar</nav>;

export default Navbar;

/* // 'use client' is needed only if we’re using React Server Components or building a library compatible with them.* */
